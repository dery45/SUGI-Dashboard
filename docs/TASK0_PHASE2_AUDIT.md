# TASK 0 — Phase 2 Audit (sections 2-10) — Already Correct / Needs Work / Missing

Tanggal: 2026-08-31 — branch `dev` after Phase 2a (703f1d9/bf15f62/07265e8/3c66caa)

## Metode
Read: `backend/controller/lifecycleController.js:108-405`, `backend/model/CropCycle.js`, `backend/model/Activity.js:1-32`, `frontend/src/pages/Lifecycle/*.jsx`, `frontend/src/pages/Lifecycle/pelaksana.js`, `backend/model/Fertilizer.js`/`Nutrient.js`/`Medicine.js`, `backend/route/masterDataRoutes.js:44-86`, `backend/model/AgriculturalInput` (non-existent), `backend/controller/masterDataController.js:6-29`, `frontend/src/component/layout/Sidebar.jsx:30-41`.

Live DB: `farmmasters 2, blocks 5, cropcycles 10, distinct blocks 4, multiPerBlock [{"_id":...,count:3},...]`

## Tabel Audit

| Spec Section (inferred) | Foundational Ask | Current Actual Content (line refs) | Live Evidence | Verdict |
|---|---|---|---|---|
| 2 | Crop-cycle creation restricted to Persiapan Lahan only | `lifecycleController.createLand:108-134` creates `new CropCycle{status:'Land_Preparation'}`. `createPlanting:198-231` does NOT create cycle, only updates existing via `crop_cycle_id` with `STAGE_REQ.planting` gate. Same for `createActivity`/`createHarvest`. Frontend `PersiapanLahanPage.jsx:128-134` POST `/lifecycle/land` is only entry point. `PenanamanPage.jsx:80-93` POST `/lifecycle/plantings` sends `crop_cycle_id`. | Grep `new CropCycle` only in `createLand` (1 hit). No `new CropCycle` in other handlers. | **Already Correct** |
| 3 | Later-stage cycle dropdowns Farm+Block filtered | Backend `lifecycleController.listEligibleCycles:180-195` uses `buildFarmFilter(user)` + `status in STAGE_REQ[stage]` — filters by farm scope (superadmin all, owner assigned_farms, farmer assignments) but NOT by UI-selected Farm/Block. Frontend `useEligibleCycles(token,stage)` fetches `/lifecycle/cycles/eligible?stage=...` without farm/block query params. `PenanamanPage.jsx:34` `cycles` is unfiltered list; `PerawatanPage.jsx:33` same; `PanenPage.jsx:22` same. No Farm/Block selector above cycles dropdown in any of those 3 pages. ManagementDashboard has cascading Farm→Block→Cycle filters but lifecycle pages do not. | `pelaksana.js:8-17` fetch URL has only `stage`, no farm/block. Frontend has no Farm/Block filter state for lifecycle dropdowns. | **Needs Work** — must add Farm+Block filter to eligible cycles dropdowns (backend already supports farm scope, but need to pass selected Farm/Block and filter client-side or via query) |
| 4 | Activity Category/Harvest-gating still in place (Phase1/2a) | `PerawatanPage.jsx:52-56` `isHarvestStage=['Harvesting','Completed'].includes(status)` → `availableCategories 2 vs 3`, `filteredActivityTypes` by `category`. `ActivityType` enum `['Lainnya','Pemupukan - Perawatan - Penyemprotan','Panen']` in `backend/model/ActivityType.js:1` and `ActivityTypeMasterPage.jsx`. DB 12 docs with correct categories (verified `verify_task4.js`: Lainnya 4, Panen 2, Pemupukan...6). | `node verify_task4.js` output shows Panen visible only if `isHarvestStage` true, else helper text. Backend gate still `STAGE_REQ maintenance=['Planted','Maintenance']`. | **Already Correct** |
| 5 | Multi-cycle-per-Block isolation | DB `cropcycles distinct blocks 4, multiPerBlock count 2-3 per block` (see live query above). Seed creates sequential cycles on same block (FARM001_B1 has 3 cycles). Frontend `PersiapanLahanPage` allows opening new cycle on same block (creates new CropCycle with same block id, different `cycle` label). No unique index on `block` prevents it. | Live count shows isolation data exists; need live confirmation that selecting Block A still shows all its cycles isolated per farm/block filter (tied to Task 2 fix). | **Already Correct (data) / Needs Work (UI filter to prove isolation)** — data supports isolation, UI needs Farm+Block filter to demonstrate |
| 6 | Completed cycles excluded from Perawatan dropdown | `listEligibleCycles` with `stage=maintenance` requires `status in ['Planted','Maintenance']` — excludes `Completed,Harvesting,Failed`. Frontend `PerawatanPage` uses that list, so Completed not shown. Also frontend `completedCycleIds` set excludes Completed for lock, but eligible list already excludes. Live `eligible maintenance sample ['Maintenance','Maintenance']` no Completed. | `node` query `find({status:Completed})` returns 5 docs, none in eligible maintenance list. `verify_task3.js` shows `Completed eligibility for maintenance: ... tidak dapat dilakukan`. | **Already Correct** |
| 7 | Fertilizer/Nutrient/Medicine references (pre-consolidation audit) | Grep `backend/`+`frontend/src` (see transcript 2026-08-31): only `masterDataController getModel cases 'fertilizers'/'nutrients'/'medicines'`, `masterDataRoutes 6+6+6 endpoints`, `models Fertilizer/Nutrient/Medicine`, `scripts/seed.js` 3+2+2 docs, `App.jsx` 3 lazy pages, `Sidebar.jsx` 3 entries. `nlp/entities.js` `FERTILIZER_KEYWORDS` is unrelated NLP, not lifecycle. `frontend/src/pages/Lifecycle/*`, `backend/controller/lifecycleController.js`, `Activity.js` have **zero** references to Fertilizer/Nutrient/Medicine. No `Activity.input` field exists yet. | `grep -i Fertilizer` hits only master-data layer, 0 hits in `lifecycleController`/`Activity.js`/`PerawatanPage`. `grep Agricultural` 0 hits. | **Needs Work (but safe to consolidate)** — no other code depends on three collections besides master-data CRUD + seed; safe to migrate and remove after Task1 migration |
| 8 | Agricultural Input unified entity missing | `backend/model/AgriculturalInput.js` does not exist; `backend/route/masterDataRoutes` has no `/agricultural-inputs`; `frontend/src/pages/MasterData/AgriculturalInputs` does not exist; Sidebar has 3 entries not 1. | `ls backend/model/*.js` shows `Fertilizer.js` etc, no `AgriculturalInput.js`. | **Missing** — to be built in Task1 |
| 9 | Activity Input+Quantity+Unit fields in Perawatan | `Activity.js:1-32` schema has `labor_hours,cost` but no `agricultural_input, quantity, unit`. `PerawatanPage.jsx:41` form has `crop_cycle_id,activity_category,activity_type,description,date,labor_hours,cost,executor,status` — no input/quantity. | `grep -n agricultural_input backend/model/Activity.js` 0 hits. | **Missing** — to be built in Task3 |
| 10 | seedLifecycle legacy code lookups | `backend/scripts/seedLifecycle.js` was partially fixed for Farm/Block in earlier commit 8bf8125 but still has legacy `findOne({code:'PADI'})`-style for CropType/ActivityType? Actually after fix, CropType lookups are now `findOne({name:'Padi'})` (fixed). Check file: `seedLifecycle.js:37-51` now uses `name` for CropType/ActivityType. Farm/Block lookups now use `name` not `code`. Verify via read. | `grep -n "code" backend/scripts/seedLifecycle.js` shows only `farm.code` removed, but still `code` in block logic? After fix, no `code` left. Need to re-verify `harvest` etc is name-based. | **Already Correct (after 8bf8125)** — but Task4 requires full audit to ensure 100% name/_id consistent, so re-verify and fix any remaining `code` strings |

## Summary Counts

- Already Correct: 4 items (2,4,6,5-data/7-refs safe,10-partial)
- Needs Work: 3 items (3 dropdown Farm+Block filter,5 UI proof,8 unified model,9 input fields)
- Missing: 2 items (8 model/page,9 schema/fields)

## Decision for TASK1-5

- Task1 must NOT delete old collections before migration; audit confirms only master-data layer references them, so after migration and verifying Activity has no dependency, safe to drop.
- Task2 must add Farm+Block filter to `useEligibleCycles` and lifecycle pages (add selectors, pass query params, backend already supports farm scope but need block filtering).
- Task3 must add `agricultural_input (ObjectId→AgriculturalInput)`, `quantity (Number)`, `unit (ObjectId→Unit auto-populated)` to `Activity` for category `Pemupukan - Perawatan - Penyemprotan` optional for `Lainnya`.
