# TASK 0 — Phase 2c Audit (spec sections 11-15 & 19) — Already Correct / Needs Work / Missing

Tanggal: 2026-08-31 — branch `dev` after Phase 2b (through commit 30e2252 / f5d4b06)

## Metode
Read: `frontend/src/pages/Lifecycle/PenanamanPage.jsx:54-164`, `backend/model/CropCycle.js:12-13`, `backend/model/CropVariety.js`, `backend/model/Activity.js:20-23`, `backend/model/HarvestPeriod.js:25`, `backend/controller/lifecycleController.js:47-405` (STAGE_REQ, eligibilityError, farm filters), `backend/controller/managementDashboardController.js:54-648` (build*Match, getBlocksByFarm, getCyclesByFarmBlock), `backend/controller/salesController.js` / `expenseController.js` (not yet read — inferred via Sale/Expense model), `backend/model/Sale.js:5` & `Expense.js:5` (`crop_cycle_id`), `frontend/src/pages/ManagementDashboard/*.jsx`, `docs/PHASE2B_REPORT.md` (deferred list). Live DB: `sales 15, expenses 60, cropcycles 10`, `sales missing crop_cycle_id 0`, `expenses missing 0`, `cycle sample variety:'IR-64' crop_type:'Padi' crop_type_ref:true`.

## Tabel Audit — Spec Sections 11-15 & 19

| Spec Section | Ask | Current Actual Content (line refs & strings) | Live Evidence | Verdict |
|---|---|---|---|---|
| 11 | Harvest+Grade integration — Penanaman captures Crop Variety (filtered by Crop Type) as prerequisite for Harvest Grade | `PenanamanPage.jsx:58` state `{crop_type, variety}` where `variety` is `<Input name="variety" placeholder="DxP, Arabika, dll" />` (free text, line 164), not a dropdown of `CropVariety` filtered by selected `crop_type`. `CropCycle.js:12-13` has `crop_type_ref: ObjectId CropType` and `variety: String` (not `variety_ref`). No `grade` fields in `CropCycle` or `HarvestPeriod`. Frontend `PanenPage.jsx` has grade-like UI? No, only `quality_grade` enum A/B/C/Reject in `HarvestPeriod.js:25`. | `grep -n "variety" frontend/src/pages/Lifecycle/PenanamanPage.jsx` → 164 free-text Input, not `cropVarieties` fetch. DB `cycle sample variety:'IR-64' crop_type_ref:true` but `variety` is string, not ref to `CropVariety`. | **Gap #1 confirmed — Needs Work / Missing** — Variety not captured as filtered master-data ref |
| 11 | Harvest+Grade: Grade selection in Panen from cycle's recorded Variety, with `estimated_price_per_unit` for value estimate | `PanenPage.jsx:22-31` `useEligibleCycles` only fetches `cycle` label, no grade fetching. `HarvestPeriod.js` has `expected_yield_kg, actual_yield_kg, quality_grade` but no `grade` or `variety_price`. No `CropVariety.grades` usage in lifecycle. | `grep -rn "grade" frontend/src/pages/Lifecycle/` → 0 hits. `grep -rn "AgriculturalInput" frontend/src/pages/Lifecycle` only in Perawatan (Phase2b). | **Missing** |
| 12 | Full lifecycle status-transition validation — consolidated state-machine (no skip, no regression, no writes vs Completed outside support), plus date-ordering where data supports it | `lifecycleController.js:50-75` `STAGE_REQ = {planting:['Planned','Land_Preparation'], maintenance:['Planted','Maintenance'], harvest:['Planted','Maintenance','Harvesting']}` + `eligibilityError` used in `createPlanting:209-210`, `createActivity:277-278`, `createHarvest:343-344`. But `updateLand:140-148`, `updatePlanting:233-240`, `updateActivity:299-306`, `updateHarvest:363-374` are generic `findByIdAndUpdate(req.body)` with `runValidators:true` but **no** `STAGE_REQ` gate, allowing status regression via direct PUT. No date-ordering validation (`land_opening_date < land_closing_date < planting_date < harvest_opening_date` etc.) anywhere. Completed-cycle lock exists only for `Perawatan` frontend `isLocked` and `createActivity` gate, but `listPlantings` shows `status in ['Planned',...,'Completed']` and `updatePlanting` could revert `Completed→Planted`. | `grep -n "STAGE_REQ\|eligibilityError" backend/controller/lifecycleController.js` → 3 uses (create* only), 0 in update* handlers. `grep -n "land_opening_date\|harvest_opening_date" backend/controller/lifecycleController.js` → no ordering checks. | **Needs Work** — existing gates are per-stage creation only, not comprehensive state-machine |
| 13 | Audit every LandRecord/CropCycle/Activity/HarvestPeriod/Sale/Expense query for `crop_cycle_id` scoping where operation is cycle-specific; farmer assignment matrix & superadmin scope hold | `lifecycleController.js`: `listLand:98` uses `farmFilterWithMaster` (farm+block), not `crop_cycle_id`; `listPlantings:162-168` uses `buildFarmFilter` + status, not `crop_cycle_id`; `listActivities:253-256` uses `buildFarmFilterSimple` (farm/block), not `crop_cycle_id`; `listHarvests:320-324` uses `farmFilterWithMaster`. Only `listEligibleCycles:180-195` filters by `status` + farm, and `create*` validates `crop_cycle_id` required. `Sale.js:5` & `Expense.js:5` have `crop_cycle_id: ObjectId` but `backend/controller/salesController.js` / `expenseController.js` not yet audited for `crop_cycle_id` scoping (need to read). `managementDashboardController.js:54-89` `buildSaleMatch/buildExpenseMatch/buildHarvestMatch` do support `cycle_id` → `crop_cycle_id` filter, but `lifecycle` list routes do not. `auth.js: isFarmerScoped` and `lifecycleController.buildFarmFilter` use `FarmerAssignment` for farmer, `assigned_farms` for owner. | Live: `sales missing crop_cycle_id 0`, `expenses missing 0`, all 15 sales / 60 expenses have `crop_cycle_id` populated → **reliability confirmed** (data is reliable, but query scoping in controller may still be farm-only not cycle-specific for some reads). `grep -n "farm_id\|block\|crop_cycle_id" backend/controller/lifecycleController.js` → `listLand` etc use only farm/block. | **Needs Work** — Sale/Expense `crop_cycle_id` is reliably populated (live 0 missing), but many list queries still scope only by `farm_id/blockId` not `crop_cycle_id`; farmer matrix holds via `buildFarmFilter` but needs re-confirmation across Sale/Expense surface |
| 14 | Crop-cycle-centric dashboard filtering — does it already exist? Farm→Block→Cycle→Period scoping | `ManagementDashboard` (Analitik & KPI) has cascading filters: `ManagementDashboard.jsx:180-193` selects Farm→Block→Cycle, `managementDashboardController.js:615-648` `getBlocksByFarm` (filter by farm scope) and `getCyclesByFarmBlock` (filter by `farm_master` + `block` + `status in [...]`), plus `calculateKPIs:92-106` and `getChartData:153-505` use `buildCycleMatch({farm_id,farm_ids,block_id,cycle_id})` → `farm_master` + `block` + `_id`, and `buildSaleMatch/buildExpenseMatch/buildHarvestMatch` all accept `cycle_id`. Frontend `ManagementDashboard.jsx` passes `filters {farm_id,block_id,cycle_id,year,start_date,end_date}` to `useManagementData`. | `grep -n "getBlocksByFarm\|getCyclesByFarmBlock\|buildCycleMatch" backend/controller/managementDashboardController.js` → all exist. Live test in TASK5: `Filtered by Farm ... 5 cycles, Filtered by Block ... 3 cycles` → multi-cycle-on-one-block test standard passes via client-side `filteredCycles` in Phase 2b (Penanaman/Perawatan/Panen modals). | **Gap #2 check — Already Correct** — crop-cycle-centric dashboard filtering **does already exist** (Farm→Block→Cycle→Period via `ManagementDashboard` + `getBlocksByFarm/getCyclesByFarmBlock`). Needs extension for Agricultural Input & Grade (see TASK 4) |
| 15 | End-to-end multi-cycle testing (two sequential cycles on one Farm/Block) | No automated test yet. `seedLifecycle.js` creates 10 cycles with 2-3 per block but not as automated e2e test. `testing/` folder exists but ignored via `.gitignore`. | `ls testing/` → ignored, no e2e test for lifecycle. `grep -rn "sequential" backend/scripts/` → only seed data, not test. | **Missing** — to be built in TASK 5 |
| 19 | Final Acceptance Criteria (Phase 2) | Spec 19 likely covers: Harvest+Grade integrated, state-machine comprehensive, data-isolation audit complete, Dashboard/KPI covers Agricultural Input & Grade, e2e multi-cycle passes, build clean, all strings Indonesian. Phase 2b deferred 5 items: Harvest+Grade, full status validation, full isolation audit, Dashboard/KPI compatibility, e2e testing. | `docs/PHASE2B_REPORT.md:7` explicitly lists 5 deferred items. `TASK1-4` in this phase will close first two gaps, leaving isolation audit + dashboard extension + e2e for TASK3-5. | **Needs Work** — Phase 2 not yet fully met (by design, deferred to 2c) |

## Summary Counts (Phase 2c TASK 0)

- Already Correct: 1.5 items (cycle creation restriction already correct from Phase 2b audit; dashboard Farm→Block→Cycle→Period exists; Sale/Expense `crop_cycle_id` reliably populated)
- Needs Work / Missing: 4.5 items (Penanaman Variety filtered missing; Grade in Panen missing; state-machine consolidation & date-ordering missing; `crop_cycle_id` scoping for cycle-specific reads missing; multi-cycle e2e test missing)
- Gaps to resolve first (per prompt): Gap #1 (Variety) — **confirmed missing**; Gap #2 (dashboard) — **confirmed already exists** (do not rebuild, only extend)

## Decisions for TASK 1-6

- TASK 1: Extend `Penanaman` to dropdown `Crop Variety` filtered by selected `Crop Type` (fetch `CropVariety` by `crop_type`), add `variety_ref` to `CropCycle` (keep string `variety` for backward compat or migrate), wire Panen Grade select from cycle's `variety`'s `grades`, snapshot price at harvest vs live reference — decide explicitly (recommend snapshot).
- TASK 2: Consolidate `STAGE_REQ` + `eligibilityError` into single `validateTransition(currentStatus, targetStage, payload)` used in all create/update handlers, block any write vs `Completed` (outside explicit `Closed→Completed` harvest finalization), add date-ordering `land_opening_date < land_closing_date < planting_date < harvest_opening_date < harvest_closing_date` where fields present, extend Completed lock to `updateLand`/`updatePlanting` (currently only Perawatan frontend lock).
- TASK 3: Audit all `LandRecord/CropCycle/Activity/HarvestPeriod/Sale/Expense` queries — add `crop_cycle_id` filter where cycle-specific (e.g., list Sales/Expenses by cycle), ensure `Sale/Expense` creation requires `crop_cycle_id` (already has, but enforce reliability), re-confirm `FarmerAssignment` matrix (`buildFarmFilter` vs `buildFarmFilterSimple` vs `farmFilterWithMaster`) holds across all surfaces.
- TASK 4: Do not rebuild dashboard — extend existing `ManagementDashboard` (`getKpi/getChartData`) to aggregate `AgriculturalInput` costs/quantities (Activity `quantity*?` and `Expense` category?) and `Grade` value breakdowns (`harvest with grade snapshot`).

